# ── Godot ───────────────────────────────────────────────
.godot/
.import/
export.cfg
export_presets.cfg
*.translation

# ── macOS / 编辑器 ──────────────────────────────────────
.DS_Store
*.swp
.vscode/
.idea/

# ── 内部文档与 AI 工作资料(不进公开仓库)─────────────────
PLAN.md
CLAUDE.md
NOTES.md
AGENTS.md
.claude/
workflow-system/
docs/internal/

# ── 测试产物 ────────────────────────────────────────────
tests/results/
# 渲染产物一律不进仓库。用通配符而不是逐个列 —— 之前只挡了 render_a/render_b,
# 新加的 render_fps30/ 就漏进去了 186 个文件 2.5MB。
tests/render_*/
*.avi
*.png.import

# 测试音频可由 tests/make_fixtures.gd 再生,不进仓库(合计约 26MB)。
# examples/hello_cue/voice.wav 是例外 —— 示例必须 clone 下来就能跑。
tests/probe/*.wav
tests/probe/*.wav.import
tests/determinism/*.wav
tests/determinism/*.wav.import
tests/determinism/fire_log.txt

# 本地运行时测量记录
runtimes.md
