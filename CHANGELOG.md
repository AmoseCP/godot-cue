# 更新日志

版本号在 1.0 之前不承诺 API 稳定。带 **⚠️** 的是破坏性变更。

---

## 0.10.0 — 2026-08-26

这一版几乎全是**正确性修复**。功能面在 0.9.0 就已经齐了,之后找到的问题
没有一个是"缺功能",全部是 bug —— 其中三个会让整集动画在你毫无察觉的
情况下出错。**建议所有 0.9.0 用户升级。**

### ⚠️ 破坏性变更

**离线时钟改用实际渲染帧率。**
`CueSheet.fps` 是编辑器概念(帧吸附、帧标尺),和 Movie Maker 实际录制的
帧率没有强制关系。旧版拿 `sheet.fps` 当离线时钟的除数,于是一个 30fps 的
sheet 按 60fps 渲染出去时,**时间走快一倍,整集动画全部提前**。
现在从固定时间步反推真实帧率,不依赖任何设置项。

> 相关:项目设置 `editor/movie_writer/fps` 在独立运行 `--write-movie` 时
> **不生效**(实测按默认 60fps 录),渲染时请显式传 `--fixed-fps`。

**`at()` 不再在 `play()` 之前 / 暂停期间立即返回。**
旧版的等待条件是 `while _playing`,而 `_playing` 在「还没 play()」
「暂停中」「已停止」三种情况下都是 false。于是这样写会让整段分镜
在一帧里跑完:

```gdscript
_shot()          # 先起协程
Cue.play()       # 再播放
```

现在判据是「这一轮播放有没有作废」,先起协程再 play 也能正常工作。

**`seek()` 不再打断挂着的 `await`。**
旧版 `seek()` 内部调 `stop()`,而 `stop()` 会唤醒所有等待者 ——
播放中拖一下播放头,所有 `await Cue.at(...)` 全部返回,分镜脚本静默崩掉。
现在向前跳时把跨过去的标记按「已经过去」逐个放行,与 `at()` 既有规则一致。

**`play()` 省略参数时从当前播放头继续。**
旧版默认 `from = 0.0`,于是 `Cue.seek(5.0); Cue.play()` 会莫名回到开头。
显式写 `Cue.play(0.0)` 仍然精确从头。播放头已在末尾时 `play()` 从头开始。

**传输条的「停止」现在真的回到起点。**
这个按钮的 tooltip 一直写着「停止并回到起点」,但代码原来不回。
想留在原处请用空格暂停。

### 修复

- **`after()` 在延迟期间暂停会提前返回**(和 `at()` 同一类错误)。
- **先 `pause()` 再 `stop()` 会让协程永远挂死** —— `stop()` 的唤醒被
  `if was := _playing` 挡着,而 pause 之后它已经是 false。现在无条件放行:
  多发一次唤醒无害,漏发一次是死锁。
- **直接改标记属性不会让排序缓存失效**。`sheet.find(&"x").time = 5.0`
  这种写法之后,`sorted()` 会返回陈旧顺序,触发队列跟着错序。
- **导入新建的轨道漏出 undo**:导入后 `Ctrl+Z`,标记没了但轨道留下。

### 新增

- **标记导航**:`[` / `]` 跳到上/下一个标记,**默认只在当前轨内跳**
  (口型轨几千个标记,跨轨跳按一次只前进几毫秒)。`Shift` 才跨全轨。
- **可搜索的标记列表**(右侧,可隐藏),同时匹配标记名和 `payload.text`。
  2320 个标记下刷新 1ms;结果超过 300 条会明说截掉了多少。
- 运行时查询:`Cue.next_marker()` / `Cue.prev_marker()` /
  `CueSheet.search()` / `Cue.is_playing()` / `Cue.sheet_loaded` 信号。
- `CueMouthShape` 换 sheet 时自动重建,不必手动调 `rebuild()`。
- 插件与资源图标;`docs/asset-library.md` 提交元数据。

### 性能

- **波形 LOD 金字塔**:一集体量(4 段音频)下整段视野的线段重建
  从 18ms/帧降到 **7ms**,且与缩放级别无关。金字塔按需在内存里建,
  不写进 `.tres`(可重算的东西不该让文本资源变大四成)。
  它是**精确的**,不是近似 —— min 的 min 就是 min。

### 工程

- **CI**:GitHub Actions 在 **Linux 和 Windows** 上跑 headless 套件。
  开发机只有 macOS,这是仓库唯一的跨平台验证途径。
- **测试 runner 不再只看退出码**。实测 Godot 对 GDScript 解析错误、
  类型错误、脚本文件不存在**一律退出 0** —— 只看退出码的话,
  任何一个编译不过的测试文件都会让套件报绿。
- 套件汇总行会报出**跳过**的断言数。第一次跑 CI 时 ffmpeg 套件因为
  runner 上没装 ffmpeg 而跳过了 24 条,却报绿 —— 那次「成功」是虚假的信心。
- 自动化断言 **759 条** + 315 帧逐帧哈希(三个场景)+ 跨帧率回归 + 10 轮插件启停。

### 已知限制

见 README 的「已知限制」一节:同一时刻只能播一个 sheet;
「分析波形」对旧 sheet 是不可逆迁移。

**M2(音画同步)仍未通过验收** —— 它的核心标准需要人戴耳机在 macOS 和
Windows 上分别实测,机器验不了。见 `tests/MANUAL.md`。这也是不标 1.0 的原因。

---

## 0.9.0 — 2026-08-25

首个公开版本。`PLAN.md` 的 P0 / P1 / P2 全部实现并有自动化验收。

- 底部面板:波形绘制、秒/帧标尺、缩放平移、多轨泳道与折叠
- 标记的增删改拖,全部走 `EditorUndoRedoManager`
- 一个 sheet 可容纳多段音频(多角色分轨配音),标记时间轴跨片段统一
- 双模时钟:实时预览跟音频走,Movie Maker 离线渲染纯帧计数
- 口型同步:Rhubarb JSON 与 MFA TextGrid 导入;`CueMouthShape` 节点;
  没有口型数据时可用振幅包络降级
- 频谱图视图(纯 GDScript FFT)、字幕文本对照
- 标记与包络的 CSV / JSON 导出;从标记生成 GDScript 剧本骨架
- MP3/OGG 经外部 ffmpeg 预转(可选依赖)
